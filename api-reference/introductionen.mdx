---
title: 'Dynamic Proxy Acquire'
version: 'English'
description: 'Introduction to the Acquire Proxy API Interface。'
---

<Note>The proxies extracted from the dynamic proxy API can only be used with whitelist authentication.</Note>

* API interface address：https://api.go2proxy.com/v1/dynamic/proxies

* Request Method：GET

* QPS Limit：Each order can be accessed once per second

## 1.Parameters

| Parameter Name       | required | Type       | Description            |
| :-------- | :- | :------- | :------------- |
| app_id   | required  | `string` | API存取憑證        |
| signature | required  | `string` | API存取憑證        |
| orderid   | required  | `string` | 訂單號            |
| num       | required  | `int`    | 本次提取IP數量（1-1000）    |
| period    | required  | `int`    | IP有效時間（1-30）      |
| region    | required  | `string` | 國家編碼，不填為`全球混播` |

## 2.回傳範例

```python 程式碼範例
{
    "code": 0,
    "msg": "success",
    "data": {
        "count": 2,
        "proxy_list": [
            {
                "ip": "43.130.57.106",
                "port": 10001
            },
            {
                "ip": "43.130.57.106",
                "port": 10002
            }
        ]
    }
}
```

## 3.回傳參數說明

| 參數名  | 類型          | 說明                           |
| :--- | :---------- | :--------------------------- |
| code | `int`       | 請求業務狀態，0為成功，非0均為業務失敗，錯誤碼詳情參下 |
| msg  | `string`    | 請求業務描述請求業務描述                 |
| data | `"" or map` | 請求業務失敗時為`空字串`                |